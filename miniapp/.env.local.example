# Localhost: point to local backend. Copy to .env.local.
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
# Optional: use deployed manifest URL so wallets can load it (Connect Wallet may work better).
# NEXT_PUBLIC_TONCONNECT_MANIFEST_URL=https://aiba-arena2.vercel.app/api/tonconnect-manifest
